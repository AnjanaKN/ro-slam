#include "headers.h"






/*	//float base_line=-0.2204503, Cx=, Cy= ,f_norm=;
    int winSize = 9;
    int minDisp = 192;
    int maxDisp = 416; // 208 224 240 256 272 304 400
*/

//branch test
    /*searchPoint.x=-0.4897;
	searchPoint.y=0.08872;
	searchPoint.z=1.394;
roi_cloud=S.get_neighbors(0, 100,0.1,cloud, searchPoint);
	S.compute_cov_matrix(roi_cloud);
	S.compute_SVD(S.C);
	S.write_singular_values();
//leaf test
	searchPoint.x=-0.0324;
	searchPoint.y=-0.1926;
	searchPoint.z=1.249;
roi_cloud=S.get_neighbors(0, 100,0.1,cloud, searchPoint);
	S.compute_cov_matrix(roi_cloud);
	S.compute_SVD(S.C);
	S.write_singular_values();
//grape test
	searchPoint.x=0.0725;
	searchPoint.y=0.1315;
	searchPoint.z=1.356;

	roi_cloud=S.get_neighbors(0, 100,0.1,cloud, searchPoint);	
	S.compute_cov_matrix(roi_cloud);
	S.compute_SVD(S.C);
	S.write_singular_values();
*/